
---

spring:
  application:
    name: shopping-service
  cloud:
    config:
      uri: http://localhost:8090
  h2:
    consoles:
      enable: true

server:
  port: 8083

jpa:
  show-sql: true
  hibernate:
    ddl-auto: update
